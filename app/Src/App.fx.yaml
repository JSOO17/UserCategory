App As appinfo:
    BackEnabled: =true
    OnStart: =ForAll(Category, Collect(CategoryList, ThisRecord))
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

